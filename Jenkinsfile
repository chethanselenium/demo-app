@Library("my-shared-library") _

pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sta = 'Build'
                printMessage(sta)
            }
        }
        stage('Test') {
            steps {
                sta = 'Build'
                printMessage(sta)
            }
        }
        stage('Deplo') {
            steps {
                sta = 'Deplo'
                printMessage(sta)
            }
        }
    }
}